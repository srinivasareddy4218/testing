node {
    
    stage('Build') {
                 if (env.Branch_Name =~ 'main') {
                echo "main"
            } 
        else{
        if (env.Branch_Name =~ 'feature') {
                echo "feature"
            }
        }
        }
}
